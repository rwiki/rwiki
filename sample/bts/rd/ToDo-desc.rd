== ToDoの使い方メモ

新規登録の際には、インデックス一番上のテンプレートページを使ってください。
「分類」および「担当」をリンクにしておくのを忘れないように。

ToDoページで[Update] するとテンプレートページが追加されます。

あるToDoをopenからcloseへ移行したときでも、
そのToDoの「分類」や「担当」のリンクは外さないでください。


== 状態

((<open>)) / ((<close>))

== 分類

((<csv>)) / ((<Div>)) / ((<rrr>)) / ((<Ruby>)) / ((<RWiki>)) / ((<SOAP4R>)) /
((<WEBrick>))

== 担当

((<gotoken>)) / ((<gotoyuzo>)) / ((<NaHi>)) / ((<ocha>)) / ((<seki>)) /
((<tosh>)) / ((<znz>))


